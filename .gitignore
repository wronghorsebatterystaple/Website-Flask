# security ignores
docker/flask/envs/*
!docker/flask/envs/.flaskenv
docker/mysql/envs
db_backup.sh
backup*.sql # SYNC: host backup path in db backup scripts

# should be ignored by default but just to be safe
# instead do pip install -r requirements.txt on clone
venv/

# Python ignores
__pycache__/
